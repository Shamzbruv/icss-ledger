console.log('To migrate the leads database schema, please run the contents of schema_leads.sql directly in the Supabase SQL Editor.');
console.log('The standard Supabase JS client does not support .query() for arbitrary SQL execution.');
process.exit(0);
