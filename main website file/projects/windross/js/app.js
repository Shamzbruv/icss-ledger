/**
 * app.js
 * Main scripts for the Marketing Site (Home).
 * 
 * Note: Configurator logic is now in js/customize.js
 *       Booking logic is in js/book.js (or embedded)
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Windross Tailoring - Welcome');

    // Init Lucide Icons if not already done
    if (window.lucide) window.lucide.createIcons();
});
